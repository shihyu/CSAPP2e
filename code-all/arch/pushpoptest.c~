/* Experiments to test pushl %esp */

#include <stdio.h>
#include <stdlib.h>

int pushtest();
int poptest();

int main()
{
  printf("pushtest -> %d\n", pushtest());
  printf("poptest -> %d\n", poptest());
}
