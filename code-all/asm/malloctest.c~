#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
    int i;
    for (i = 1; i < 10; i++) {
	void *p = malloc(i * sizeof(int));
	printf("Allocated %d.  Address = %p\n", 4*i, p);
    }
    return 0;
}
